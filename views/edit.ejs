<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Task</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: '#6366f1',
            'primary-dark': '#4f46e5',
            dark: {
              900: '#0f172a',
              800: '#1e293b',
              700: '#334155',
            }
          }
        }
      }
    }
  </script>
  <style>
    body {
      background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
      min-height: 100vh;
    }
    
    .card {
      background: rgba(30, 41, 59, 0.7);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.05);
    }
    
    .input-field:focus {
      box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.3);
    }
  </style>
</head>
<body class="text-white font-sans">
  <div class="max-w-3xl mx-auto min-h-screen px-4 py-8 flex flex-col justify-center">
    <!-- Card Container -->
    <div class="card rounded-2xl shadow-xl overflow-hidden">
      <!-- Header with gradient -->
      <div class="bg-gradient-to-r from-dark-800 to-dark-900 p-6">
        <h1 class="text-3xl font-bold flex items-center">
          <i class="fas fa-edit text-blue-400 mr-3"></i>
          Edit Task
        </h1>
      </div>
      
      <!-- Form Content -->
      <div class="p-6">
        <form action="/files/<%= encodeURIComponent(oldTitle) %>/rename" method="POST">
          <div class="mb-6">
            <label class="block text-zinc-400 mb-2 text-sm font-medium">
              <i class="fas fa-heading mr-2"></i>Task Title
            </label>
            <input 
              type="text" 
              name="title" 
              value="<%= oldTitle.replace('.txt', '') %>"
              class="w-full input-field bg-dark-700 border border-zinc-700 rounded-lg px-4 py-3 focus:outline-none focus:border-blue-500"
              required
            >
          </div>
          
          <div class="mb-8">
            <label class="block text-zinc-400 mb-2 text-sm font-medium">
              <i class="fas fa-align-left mr-2"></i>Task Details
            </label>
            <textarea 
              name="task_details"
              rows="6"
              class="w-full input-field bg-dark-700 border border-zinc-700 rounded-lg px-4 py-3 focus:outline-none focus:border-blue-500"
              required
            ><%= task_details %></textarea>
          </div>
          
          <div class="flex justify-end space-x-3 pt-4 border-t border-zinc-800">
            <a href="/files/<%= encodeURIComponent(oldTitle) %>" class="px-6 py-3 bg-zinc-700 hover:bg-zinc-600 rounded-lg transition-colors flex items-center">
              <i class="fas fa-times mr-2"></i> Cancel
            </a>
            <button type="submit" class="px-6 py-3 bg-blue-500 hover:bg-blue-600 rounded-lg transition-colors flex items-center">
              <i class="fas fa-save mr-2"></i> Update Task
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</body>
</html>